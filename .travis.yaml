sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t kasbuunk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kasbuunk/docker-react npm run test -- --coverage
